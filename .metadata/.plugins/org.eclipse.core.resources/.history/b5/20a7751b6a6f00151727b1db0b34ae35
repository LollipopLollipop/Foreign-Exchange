import java.util.ArrayList;
import java.util.Collections;


public class RandomForest {
	private int N = 0;
	private ArrayList<DecisionTree> tree = new ArrayList<DecisionTree>();
	public RandomForest(int N){
		this.N = N;
	}
	public void buildForest(ArrayList<CandleStick> instances) {
		for(int i=0; i<this.N; i++){
			Collections.shuffle(instances);
			ArrayList<CandleStick> testingInstances = 
					new ArrayList<CandleStick>(totalInstances.subList(0, (int) (TOTAL_INSTANCES*0.2)));
		}
		
	}
}
